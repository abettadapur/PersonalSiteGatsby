import Typography from 'typography';

const typography = new Typography({
    baseFontSize: '14px',
    headerFontFamily: ["Segoe UI Web (West European)", "Segoe UI", "Roboto", "Helvetica Neue", "sans-serif"],
    bodyFontFamily: ["Segoe UI Web (West European)", "Segoe UI", "Roboto", "Helvetica Neue", "sans-serif"],
});

module.exports = typography;
